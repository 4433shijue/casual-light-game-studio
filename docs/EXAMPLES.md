# 复制一句，开始做

下面是需求示例，内容可以任意改。它们没有预订你的创意名额。

## 想先聊聊

```text
使用 $casual-light-game-studio。
想做一个轻松的整理小游戏，物件之间有故事联系。
先给几个有区别的体验方向，我选完再细化。
```

## 想马上玩起来

```text
使用 $casual-light-game-studio。
做一个浏览器里的接雨滴小游戏，用鼠标移动杯子。
直接实现最小可玩版，先用几何图形，能开始、结束和重玩就行。
技术按这个目标推荐，别为了用模板改我的玩法。
```

## 完全没有胜负也行

```text
使用 $casual-light-game-studio。
我想做一个用鼠标吹动云朵的小品，没有分数和失败。
先验证云的移动与反馈是否舒服，暂时不加关卡。
```

## 让外部模型帮忙

```text
使用 $casual-light-game-studio。
我的场景风格已经确定，需要三种物件和一段结尾视频。
先读现有美术约定，再给生成提示词、规格和回传要求。
当前只有文本能力，不要声称已生成素材。
```

## 今天只改一个地方

```text
使用 $casual-light-game-studio。
现有项目里触屏点击一次会计分两次。
只排查并修复这条输入链路，完成后验证鼠标和触控。
```

## 我改主意了

```text
使用 $casual-light-game-studio。
保留原来的技术和美术，玩法从限时挑战改为自由探索。
先分析受影响的规则与界面，更新接续记录，等我要求再改代码。
```

## 给世界取一套合适的称呼

```text
使用 $casual-light-game-studio。
我的游戏是幽灵邮局，玩家夜间送信，有委托和路线，没有战斗、等级或体力。
请直接设计身份称呼、任务列表、路线菜单和首轮教学，温柔但有一点夜班幽默。
说明命名理由，区分候选和采用稿，不要增加玩法；缺少的信息标明假设。
```

## 只改三个词

```text
使用 $casual-light-game-studio。
这是阳台茶摊经营游戏，只给订单、金钱、顾客关系各想一个叫法。
金钱用于买原料，顾客关系不能花费。口吻日常，别写完整策划，也不改代码。
```

## 已有文案打架了

```text
使用 $casual-light-game-studio。
我的任务奖励里“余烬”是货币，角色面板里“余烬”却是经验。
保留货币名称，检查混淆并提出经验名称，列出需要同步修改的文案位置。
只给建议，不改代码、资源路径和存档字段。
```

## 安静一点也好

```text
使用 $casual-light-game-studio。
游戏只有鼠标拖动色带、滚轮调风速，没有角色、关卡和胜负。
请写两句操作提示，以及重置画面、声音设置、退出三个按钮的文字。
保留容易理解的普通词，不要加剧情或养成系统。
```

## 不想先安装一堆工具

```text
使用 $casual-light-game-studio。
我没有游戏引擎，也不想安装工具。做一个简单的整理小游戏原型。
优先考虑浏览器能运行的交付，说明我和朋友各自需要准备什么。
```

## 发一个文件就能玩

```text
使用 $casual-light-game-studio。
做一个离线单文件 HTML 小游戏，直接双击打开，不依赖 CDN 或外部素材。
直接实现最小体验，并实际检查文件打开方式，说明未验证的设备。
```

## 横屏桌面操作

```text
使用 $casual-light-game-studio。
横屏调饮料小游戏，用鼠标直接操作桌面器具，只支持横屏桌面。
别给我摇杆和攻击键，也不用另做竖屏玩法。按桌面空间设计界面。
```

## 竖屏单手整理

```text
使用 $casual-light-game-studio。
做竖屏单手整理小游戏，常用操作方便拇指够到，物件不要被控件遮挡。
先按这个动作设计布局，案例中的顶部资源条和底部按钮不用照搬。
```

## 只调一个动作

```text
使用 $casual-light-game-studio。
我的整理游戏拖动物品很跟手，但松开后突然吸到格子里，感觉生硬。
只分析这个动作的手感，给小范围改法与复测方式，不写代码，不加新系统。
```

## 带着试玩记录找原因

```text
使用 $casual-light-game-studio。
一次试玩中，玩家连续三次把物品拖到同一个位置，物品每次都回到起点。
我还没确认是落点判定还是规则理解问题。只分析可能原因和下一步观察方法。
不要把你的推测当成玩家原话或已经确定的原因。
```

## 自由体验也能写方案

```text
使用 $casual-light-game-studio。
这是没有胜负和终点的声音互动作品。帮我整理体验结构与试玩观察点。
不要增加计分、关卡或重玩目标，模板里不适用的部分直接跳过。
```

## 换模型接着写

```text
使用 $game-copywriting-studio。
读取我的设定、术语表和采用稿。保留角色称呼、人物关系、机制数值和变量。
满意样句只作风格参考。本次只改措辞，不新增剧情或功能。
```

## 茶摊会不会没钱继续做

```text
使用 $casual-light-game-studio。
初始10枚钱，每杯先付6枚材料费再立刻收到5枚，没有其他收入或库存。
分析连续制作多久会卡住，说明假设和候选修复。本次只分析，不改规则。
```

## 把当前作品交给朋友

```text
使用 $casual-light-game-studio。
游戏目前只验证了桌面浏览器，触屏和离线还没测。
根据当前实现写简短玩法介绍、操作与启动说明，再列录屏镜头清单。
不把待实现能力写成完成，不上传，不替我发送消息。
```
