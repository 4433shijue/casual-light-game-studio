# 验证记录

## v1.2.1 数值推演与作品交付

2026 年 9 月 22 日，一名独立执行者读取更新后的入口与两份参考，实际回答四类文本请求。维护者复核输出，没有将同次执行称为跨模型测试。

| 请求 | 实际观察 |
| --- | --- |
| 茶摊先付 6 枚再收 5 枚，初始 10 枚 | 算出完成 5 杯后剩 5 枚，第六杯无法开始；修复明确为候选，允许保留有限预算设计 |
| 唯一钥匙位于唯一锁门后 | 根据给定前提判断循环依赖，没有添加替代入口 |
| 无胜负声音互动的两句介绍 | 仅描述拖动发声与静音，保留桌面浏览器测试范围 |
| 手机与离线尚未实现的作品集材料 | 如实标为后续计划；提供待录制镜头清单，未声称已录制或上传 |

另用 Python 实际核算茶摊余额序列为 10、9、8、7、6、5，并检查第六次无法支付。该计算验证示例规则，不证明实际游戏乐趣或平衡性。

两个 Skill 格式、包内链接、共享资料一致性和敏感信息扫描通过。两个 ZIP 重复构建内容一致，并逐文件核对源目录与本地安装副本。新增参考和指南经过文风检查，表格与教程的句长提醒人工复核。

独立文案能力未改，本轮仅同步套件版本说明，没有重复文案行为试用。游戏骨架未改，没有重新执行引擎测试，也没有真实玩家测试、视频录制或具体游戏部署。历史文件双击、断网与真机未验证项继续保留。

## v1.2.0 独立文案工作室

2026 年 9 月 21 日，分别通过小游戏工作室和独立文案工作室两个入口进行试用，每个入口由一名独立执行者处理同一组六个请求。比较遵守约束和范围，不要求文字相同，也没有将它当作不同模型之间的评测。

| 请求 | 两个入口的实际表现 |
| --- | --- |
| 只改一句缺料提示 | 均只给出“茶叶不足”，没有展开策划 |
| 邮差面对熟人与陌生人 | 关系改变称呼和表达，均未编造修复日期 |
| 首次与重复缺料提示 | 保留 {missing}，并注明切换机制尚未实现 |
| JSON 机制说明 | 保留 brew_help、{count}、消耗3片茶叶与恢复体力效果 |
| 换模型接续术语 | 经验继续叫阅历，保留 {amount}，没有把幽默样句变成新剧情 |
| 无剧情色带 | 只交付拖动提示与退出按钮，没有新增人物或故事 |

独立执行者仅读取独立包内入口与参考完成任务，没有依赖主工作室外部资料。两个入口的称呼与机制保持一致，部分措辞不同。

两个 Skill 格式检查通过。打包测试覆盖共享资料漂移检测与修复、包内链接、重复构建得到相同 ZIP、已有输出目录防覆盖，以及拒绝在源目录内生成附件。独立包不含游戏骨架或运行脚本，用户不需要额外写作 Skill。

共享资料由主包维护，独立副本含来源注释。安装、指南和新参考经过写作检查；句长提醒按教程和表格用途人工复核。

这次只验证文本任务、资料和打包过程，没有真实玩家评价、游戏界面或触发运行时测试。不保证所有模型相同效果，也未验证所有智能体的技能自动发现。游戏骨架没有改动，历史本地文件打开、断网和真机未验证项仍保留。

## v1.1.2 手感与试玩

2026 年 9 月 21 日，生成脚本测试通过，覆盖 experience、旧 loop、同时提供时的优先级，以及缺失、空白、非文本值。所选 experience 无效时明确报错，不回退到 loop。另检查未知依赖、自循环、重复模块及已有目录防覆盖。

实际生成自由色带的方案、任务与试玩表，体验描述没有终点，任务保持未勾选，试玩表保持未测并允许不适用。人工核对没有强制添加胜负或重玩机制。

一名独立执行者在同次试用中分别处理四个请求，输入没有给出预期答复，维护者复核实际输出。

| 请求 | 输出观察 |
| --- | --- |
| 无胜负声音互动 | 保留随时进入与离开的体验，观察动作与声音关系，未增加计分关卡 |
| 只分析吸附生硬 | 保留原判定，建议短距离缓动与候选时长，并说明长距离吸附需要另查，没有修改工程 |
| 三次放置都回到起点 | 分开命中、规则理解与取消事件等候选原因，给出区分办法，没有认定单一病因 |
| 只有有点无聊 | 建议定位具体片段和玩家期待，再试小改动，没有直接添加奖励系统 |

两份新参考和公开指南通过写作检查，句长提醒结合教程结构人工复核。Skill 格式、相对链接、文档示例命令和发布扫描通过。包级一致性在打包及发布后核对。

本次没有更改游戏骨架或执行真实玩家试玩，未重复引擎运行测试。下方历史未验证的本地文件打开、断网和真机项目仍保持未验证。

## v1.1.1 浏览器与自由布局

2026 年 9 月 21 日，原生 HTML 示例通过本地 HTTP 服务在浏览器运行。观察 1280×720 横屏和 390×844 竖屏截图，侧面说明在竖屏移到上方，控件未遮挡色带。实际操作检查了鼠标拖动、暂停、继续、暂停中重置，以及方向键、空格和 R 快捷键。

Node 隔离运行脚本测试通过，覆盖指针取消、第二指针隔离、暂停后冻结、恢复、重置保持暂停、键盘、缩放后坐标和减少动态效果的初始状态。其中触摸形态的 PointerEvent 参数由测试模拟，不等于浏览器触控模拟或真实手机测试。

源码检查没有外部脚本、字体、素材请求或网络调用。浏览器工具的 URL 安全策略拒绝 file 协议，本次未绕过限制。因此本地文件直接打开和真实断网运行尚未验证，HTTP 服务下通过不能代替这两项。手机文件打开、真实触控与所有浏览器兼容性也未验证。

html、godot、phaser 三种模板复制和已有目录拒绝覆盖测试通过，拒绝后原文件内容保持不变。未改原有引擎骨架，本次不重复其运行测试。

一名独立执行者在同次试用中分别答复五个请求，输入未提供预期答案，维护者复核实际输出。

| 请求 | 观察 |
| --- | --- |
| 不装引擎的小原型 | 推荐原生 HTML，区分制作与运行条件，未要求安装引擎 |
| 单文件离线分享 | 提出内嵌资源和文件方式、断网验收，未冒充已经测试 |
| 横屏直接操作器具 | 以桌面器具为主要交互，没有套用摇杆攻击键 |
| 竖屏单手整理 | 给出拇指可达、点选替代长拖动等建议，标明待真机验证 |
| 只支持横屏 | 保留横屏范围，处理窄窗口，不要求另做竖屏玩法 |

格式、链接、文档命令与发布扫描沿用包级检查。文本行为试用不代表所有模型或所有生成游戏都能达到同样效果。

## v1.1.0 文案分支

2026 年 9 月 21 日，Skill 格式检查、相对链接和发布内容的敏感信息扫描通过。README、指南、新增文案参考与术语模板经过写作检查；句长相似的提醒人工复核后保留，表格与操作说明按易读性处理。

一名独立执行者在同次试用中读取更新后的 Skill 和文案参考，分别完成下列四个请求。输入提供任务与设定，没有提供预期答复或评分标准。四份输出由本次维护者复核，不代表四个独立模型的测试。

| 实际请求 | 输出观察 |
| --- | --- |
| 幽灵邮局整套称呼与首轮教学 | 给出夜班邮差、投递委托和路线菜单；明确交互假设，示例人物与地址标为虚构，没有新增体力或战斗 |
| 茶摊只改三个名称 | 仅给茶单、茶钱和熟络度，并区分可花费货币与关系，没有重新策划 |
| 货币与经验都叫余烬 | 保留货币，建议经验改为阅历；列出相关显示文案和资产提示词，未改代码或存档 |
| 无角色无关卡的色带互动 | 只给两句操作提示及重置画面、声音设置、退出三个按钮，没有添加成长系统 |

这些是文本任务试用，没有真实游戏界面可供检查，尚不能证明实际字体、换行与布局适配。本次未改游戏骨架，也未重新执行下方的历史引擎测试。附带 Python 工具的文档示例命令重新执行通过。

发布包制作时核对源目录、本地安装副本与 ZIP 的逐文件内容。GitHub 同步后另核对提交树、版本标签和下载附件，发布结果由交付记录说明。

## 历史验证

下面记录实际做过的检查，时间为 2026 年 9 月 14 日。仓库发布时另检查文档链接与示例命令。后续修改需要根据影响重新验证。

## 工具与模板

四个 Python 工具经过正常输入、错误字段类型、重复 ID、依赖循环、任务排序、防覆盖和复制一致性检查。

Phaser 在 Node 24 下通过核心玩法测试，并在浏览器观察过点击计分、目标变化、超时失败、重玩与成功结算。Godot 4.6.3 完成无界面场景启动和玩法测试，包含暂停、成功、失败、结束后输入和多次重置。

Godot 尚未完成可视化人工试玩，各平台导出也没有全部验收。自动测试能证明部分行为正确，无法替玩家决定好不好玩。

## Skill 行为试用

三个未继承开发讨论历史的独立执行者处理了四个具体请求。没有给它们评分标准或预期答案，随后检查实际回复与产物。

| 请求 | 观察 |
| --- | --- |
| 只修暂停扣时间 | 只增加暂停判断，没有重新策划；另用 Node 检查扣时、暂停和恢复 |
| 无计分风铃小品 | 保留声音与自由体验，没有强加关卡或结算 |
| 故事关联的合并解谜 | 保留自定义规则，并在缺少生成工具时给出外部交接建议 |
| 旧记录与代码冲突，用户又改方向 | 更新决定，纠正完成状态，明确历史日志与本次未执行的区别 |

两个创意请求由同一执行者分别回答，其余请求独立执行。这些场景提供了有限证据，不能推断所有模型都能同样处理。

## 素材状态

提示词均为可编辑示例。本次没有调用图片、音频或视频模型生成正式素材。格式、版权来源与实际游戏内表现，要在真正制作和接入时分别检查。
