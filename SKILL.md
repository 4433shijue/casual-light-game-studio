---
name: casual-light-game-studio
description: "从一个小游戏想法出发，规划并推进休闲轻量小游戏的核心乐趣、完整游玩流程、最小可玩版本、技术框架、游戏资产和实现验证，支持浏览器与单文件 HTML、横屏或竖屏体验，也支持独立的世界观用语、游戏文案设计与检查。默认先输出方案并从概念验证版开始；用户明确要求时可切换为边设计边实现或先做可玩骨架，并支持逐步升级到展示 Demo 或小型完整作品。适用于小游戏策划、原型、资产规划、实现和迭代，不适用于大型商业游戏制作或与游戏无关的软件项目。"
---

# 休闲轻量小游戏工作室

把一个模糊的小游戏想法转成可执行、可验证、可持续扩展的制作方案，并在用户要求时继续参与实现、资产生产和版本升级。默认目标是尽快得到一个概念验证版，同时保留从 A 到 B、C 的升级路径。

## 创意与参考资源的关系

以用户的创意、目标和当前需求为依据，允许智能体主动提出新玩法、跨类型组合、不同技术结构和新的制作方法。案例、模板、提示词、检查清单、脚本以及 starter project 都是可选参考，既不穷举方案，也不定义创作上限。

- 可以选择性采用、改写、组合或完全跳过附带资源，不需要为不套模板另行请求许可。
- 用户已经确定的创意应保留；智能体提出的新方向应标为建议，解释它怎样服务目标，不擅自替换已确定的方向。
- 模式和 A/B/C 版本只是方便沟通的默认起点，可按项目目标重排、合并或跳过阶段。用户未提出其他要求时，仍默认方案优先、先做概念验证、资产清单与提示词优先。
- 允许从氛围、故事、视觉、声音、交互实验或技术机制切入，再寻找合适的体验结构。无需强行套用重复循环、分数、关卡、失败和重玩。
- 文档章节、案例数字、资产规格和目录结构都可以调整。只保留对当前作品有帮助的部分，不为填表增加系统。
- 脚本只检查它声明支持的数据格式，检查结果不评价创意质量。项目采用其他合理结构时，可以调整工具或使用其他验证方式，不能为了通过示例脚本改变玩法。

这种自由不改变事实和执行边界：尊重用户约束与授权，如实报告工具能力、生成结果和验证状态，对实际交付做与其风险相称的检查。

## 总体原则

- 围绕当前创意明确希望玩家获得的体验；适合循环玩法时分析重复动作与乐趣，其他作品可从探索、表达、情绪或一次性体验展开。
- 默认先做小范围、可玩的概念验证版。除非用户明确要求，暂不擅自增加完整剧情、大量关卡、复杂成长、联机、商业化或发布运营系统。
- 根据用户目标推荐引擎、平台和技术栈。不要因为熟悉某个引擎就强行使用它。
- 方案、已实现、已生成、已验证必须分开描述。没有实际执行的代码、资产生成、导入、运行和测试不能声称完成。
- 继承用户明确的风格、平台、时间、技术和内容约束。未知内容可以给出假设，但要标为假设并说明如何替换。
- 增量改动检查与本次影响有关的路径，例如输入、状态、存档或资产；没有关联的模块不重复审计。
- 涉及外部生成服务、账号、发布平台或真实文件的操作，遵循当前授权范围；没有授权时输出可交接的方案和提示词。

## 工作模式

### 需求补全与开工确认

用户只给出泛泛想法、目标或愿望时，先进入需求补全阶段。读取 [需求补全与开工确认](references/intake-and-confirmation.md)，把理解、关键假设、推荐方向、范围边界、待确认取舍和确认后交付物讲清楚。此阶段不创建文件、不改代码、不生成正式资产、不运行会改变项目状态的命令。

方案末尾明确写出当前等待用户确认。只有用户说“按这个方案做”“开始实现”“直接改”“继续落地”等明确授权后，才开始制作、写文件、生成或接入资产、运行测试、打包、安装或提交。单独的“可以”“不错”在执行对象不清楚时只触发一次简短确认。用户明确要求直接做或不要问时，可跳过这一轮，但要先列必要假设。

方案确认不等于外部发布、上传、发消息、登录或付费生成服务授权。需求中途改变核心玩法、平台、范围或交付物时，先重新对齐影响，再继续；小的文案和参数调整可沿用当前授权。

先判断本次任务的大小，再决定工作方式。小改动、修复和接续工作不重新启动完整策划流程，具体取舍参考 [任务范围与项目接续](references/task-continuity.md)。只读取与当前问题有关的文件和参考。

新游戏且用户尚未要求实现时，默认使用 **A：方案优先**。先交付适量方案。用户已经要求实现、修复或增加功能时，直接在授权范围内推进，不另设方案确认关卡。

当用户明确提出“先做能玩的”“先搭骨架”“直接实现核心玩法”等要求时，切换到 **C：先做可玩骨架**。先实现或指导实现最小核心循环，验证该玩法实际需要的启动、输入、反馈与结束或持续游玩路径，再补设计文档、资产和扩展。

当用户要求边做边定、逐个模块落地或同步给代码任务时，切换到 **B：边设计边实现**。每确定一个小模块，就给出该模块的目的、输入输出、实现任务、依赖和验证方式；不要等待整份总方案完成才开始推进。

必要时简短说明工作方式；小任务无需报告模式标签。用户可以随时切换模式，保留仍然有效的已确认内容。

## 版本目标

### A：概念验证版（默认）

验证最值得探索的体验假设。以下是常见参考项，仅选取适用部分：

- 玩家目标和一次循环中的主要动作
- 开始、游玩、成功、失败和重玩路径
- 最少的输入、规则、反馈、场景和必要资产
- 一个可观察的验证标准，例如能否顺利完成一轮、玩家是否知道下一步、核心动作是否有即时反馈

内容量优先保持最小。可以使用占位图形、文本、简单音效或临时素材，但必须记录后续替换点。

### B：可展示 Demo 版

在 A 已经可玩且核心乐趣没有明显问题后升级。根据用户目标补充基础 UI、教学、视觉反馈、失败与重玩体验、有限的内容变化、音效和稳定的展示流程。明确哪些内容是展示需要，哪些内容仍可延期。

### C：小型完整作品版

在用户确实需要更完整交付时规划。可包含较完整的关卡或内容结构、成长、设置、存档、性能、无障碍、平台适配、发布包和验收记录。每次升级都先列出新增目标、影响的现有模块、需要补充的资产和验证项目，再执行或指导实现。

不要把 B 或 C 当作自然终点。支持持续添加新关卡、规则、角色、资产、模式和平台，但每次都进行范围和回归检查。

## 从想法到方案

收到想法后，按问题复杂度整理必要信息。下面是可裁剪的摘要参考，不限制篇幅、顺序或章节：

1. 一句话玩法和目标玩家。
2. 核心乐趣假设，以及玩家反复执行的动作。
3. 一轮游玩流程：进入、教学或提示、主要操作、反馈、判定、奖励或失败、重玩。
4. 概念验证版范围与明确排除项。
5. 推荐平台和技术框架，以及选择理由和替代方案。
6. 系统、场景、输入、数据、UI、音频、美术和特效的最小清单。
7. 实现顺序、依赖关系、验收标准和风险。
8. 下一步最小行动。

需要具体范例时，按玩法类型读取 [案例库](references/casebook.md)；需要快速起草时，可参考 [方案模板](references/plan-template.md)。不要默认读取全部参考文件。

需要比较引擎或平台时，读取 [技术栈推荐矩阵](references/tech-stack-matrix.md)；需要补齐常见系统时，读取 [玩法系统库](references/gameplay-systems.md)。

如果关键想法不清楚，可以用三选一帮助收敛。每次只问一个会改变方案的问题，三个选项要代表不同的玩法或制作取舍，并接受自由补充。不要为了填满流程而重复追问已经明确的信息。

## 环境、交付方式与画面布局

根据玩法、现有工具、安装意愿和分享方式选择技术，不默认 Godot 或前端工程。只检查相关环境；用户不想安装工具时优先评估原生网页或单文件 HTML。已有项目先沿用，缺少另一引擎不是迁移理由。制作端与玩家端的依赖分别说明，交付时写清如何启动、是否需要本地服务、联网需求和验证范围。需要浏览器或离线交付时读取 [浏览器交付指南](references/browser-delivery.md)。

横屏、竖屏、可旋转或窗口自适应按设备、视野和操作需求选择，用户只要一种方向时不强加另一种。先确定玩家动作、关注区域与必要信息，再安排界面和输入；适配需要时重排布局，不只缩放原画面。代码职责可以复用，示例玩法、屏幕方向、摇杆和 HUD 不能自动成为产品需求。需要布局决策时读取 [屏幕方向与交互布局](references/layout-and-input.md)。

## 推荐技术框架

根据目标推荐，而不是预设引擎。可结合实际需要考虑：目标平台、输入方式、2D/3D、是否需要物理、内容规模、资产类型、用户现有经验、发布渠道、性能和迭代速度。

输出推荐时说明：

- 首选技术栈及适用原因
- 仍可接受的替代方案
- 会改变选择的约束
- 概念验证版的最小项目结构
- 后续升级可能产生的迁移成本

浏览器、桌面、移动端或多平台需求要分别说明运行与导出差异。没有实际项目目录时先给结构建议；有项目目录时先检查现有框架和入口，再提出改动。

## 游玩流程与游戏框架

把流程写成玩家可观察的状态和转移，而不是只写功能名。可参考启动、提示、操作、反馈、目标达成、暂停、退出、结算、重玩和保存等状态，只保留符合玩法的部分。无失败、无分数或持续开放的体验应采用自己的流程。

把框架拆成与实际玩法相关的模块，例如输入、游戏状态、规则判定、计分或资源、关卡/内容、角色或敌人、摄像机、UI、音频、资产加载、存档和调试。没有必要的模块不要硬加。

对每个模块说明职责、依赖、玩家可见结果和最小验证方式。优先保证模拟逻辑与渲染、UI、资产之间边界清楚，便于替换占位资产和扩展新内容。

## 世界观用语与游戏文案

需要整套文案、局部命名或已有文案检查时，读取 [世界观用语与游戏文案](references/game-copywriting.md)。这个分支可单独调用，不要求先做完整策划。根据当前设定和真实功能设计称呼、界面提示、教学、反馈及角色和物品说明，保留用户已定创意；不为套用案例增加系统。

方向不明时可提供三种不同的语言方向；用户要求直接生成时，明确必要假设后交稿。机制、数值、保存与操作后果必须准确，世界观表达不能妨碍理解。持续项目可选用 [术语与文案记录](assets/templates/game-lexicon.md)，优先沿用已有记录。改名检查相关文案和资产提示词，仅修改显示文案不自动改变代码标识或存档字段。

## 资产规划与多模态生成

默认提供 **B：资产清单 + 生成提示词**。按角色、场景、道具、UI、图标、特效、动画、音效和音乐分类，记录用途、数量、尺寸/比例、格式、透明背景、视角、状态、锚点、命名和导入要求。

对适合生成的项目给出可复制提示词，包含主体、风格、构图、视角、背景、尺寸、透明度、状态一致性、禁止项和用途。需要连续动画或多张一致资产时，补充参考图、锚点、帧数、命名和统一规格要求。提示词要服务于当前游戏，不泛泛描述“好看”“高级”。

检查当前智能体和工具实际支持的能力：

- 支持图像生成时，可以执行参考方向、首轮样片、候选筛选、尺寸/格式检查、统一规格、导入和运行检查；每一步都报告真实结果。
- 支持视频或音频生成时，可以按同样方式规划和检查，但不能把图像能力当作视频或音频能力。
- 不支持某类生成时，输出外部制作交接包：素材规格、提示词、参考要求、文件命名、目录位置、导入步骤、验收标准。用户外部生成并放回项目后，再继续接入和验证。
- 生成的候选资产在用户确认前保持候选状态，不自动替换已有正式资产或批量扩展全部内容。
- 外部资产必须检查格式、尺寸、透明度、帧数/时长、命名、版权或来源信息（如用户提供）以及在目标运行环境中的显示结果。

资产清单要区分概念验证版必需项、可用占位替代项、Demo 版补充项和完整作品版补充项，避免第一版被资产生产拖慢。

需要提示词结构、命名和交接字段时，读取 [资产生产手册](references/asset-production.md)。

角色声音、情境与重复提示，以及轻量分支对白的指导包含在上述文案资料中，按需读取即可。本包独立具备这些能力，无需另装文案 Skill。

## 手感反馈与试玩诊断

调整输入响应、吸附、动画或声音时按需读取 [手感与反馈](references/feel-and-feedback.md)。根据实际动作和希望获得的感受选择表现，不默认添加震屏、粒子或震动，也不以动画掩盖判定错误。

解释试玩问题与迭代方向时读取 [试玩诊断](references/playtest-diagnosis.md)。区分真实观察、玩家原话、假设和工具证据，提出有依据的小改动及复测方式；没有真实试玩不编造满意度。两个分支均可独立使用，小任务不强制生成整套文档，按用户的分析或实现请求控制范围。

## 实现、验证与增量迭代

按本次请求推进，说明影响结果的前置条件、改动范围与验证方式。小修复可以用简短说明完成，不强制生成任务表或全套设计文档。

概念验证版根据体验目标确定验收：例如能启动、输入可用、反馈明确、适用规则正确；仅当玩法具备成功、失败和重玩时检查这些路径。若存在资产或平台依赖，分别记录静态检查、模拟运行、真实运行和未验证项。

新增内容时按影响记录必要变化；持续项目的重要变化可更新现有接续记录，微小改动在回复中说明即可。记录可包括涉及模块、数据兼容、资产、回归风险和验证结果，只选相关项。新增关卡或内容可采用数据驱动或可替换结构，避免引入超过当前需要的复杂度。

发现问题时先判断是玩法、流程、输入、规则、表现、资产、平台还是工具能力问题，再提出最小修复。不要用增加功能掩盖未解决的核心循环问题。

升级或加入内容时，可参考 [版本升级清单](references/upgrade-checklist.md)。仅当项目采用示例资产清单格式且检查有帮助时，使用 `scripts/check_game_manifest.py`；目录存在本身不是采用该格式或执行脚本的理由。

准备交付可运行包时，可按目标平台参考 [发布前检查清单](references/release-checklist.md)。需要建立初始资产清单时，可运行 `scripts/scaffold_manifest.py` 生成模板，再按项目实际内容修改。

## 轻量数值与规则推演

检查资源收支、成长门槛、规则矛盾或关卡可达性时，按需读取 [轻量数值与规则推演](references/balance-and-rules.md)。以当前规则与实现为依据，明确假设、单位和计算范围；简单问题直接计算，重复或随机过程再按实际需求使用脚本。区分规则错误、设计取舍与证据不足，不能用模拟证明好玩。没有数值需求的作品直接跳过。

## 作品展示与交付

准备给朋友试玩、作品集或现场演示时，读取 [作品展示与交付](references/showcase-and-delivery.md)。依据已实现内容整理介绍、操作、截图录屏清单、启动方法与限制，按任务大小裁剪。概念图不当作实机截图，待录制不写成已完成。只在现有授权内进行外部发布或发送，本地产物准备好不等于已经公开可用。

## 交付格式

方案可根据实际需要选取以下内容，或采用更合适的表达方式：项目摘要、核心乐趣、游玩流程、版本范围、技术推荐、游戏框架、资产清单与提示词、实现顺序、验证标准、风险和下一步。

用户要求代码或实际制作时，补充文件路径、实现任务、运行命令、依赖、变更说明和测试结果。用户要求资产时，补充候选文件、规格检查、导入状态和仍需人工确认的部分。

如果只是方案阶段，不生成虚构的完成报告。明确“计划”“候选”“待用户提供”“已检查”和“已运行”。不把外部生成、安装、发布或跨平台兼容写成已经完成。

## 按需加载的制作工具

- 持续项目需要交接时，可借用 [项目接续记录](assets/templates/project-context.md)，优先更新已有同类记录。单次咨询不自动创建文件。接续时核对相关实现与证据，旧记录不能替代当前事实或用户新要求。
- 修改本 Skill 后需要行为评测时，可参考 [使用评测场景](references/behavior-evaluation.md)。只选受影响的场景；不把评测作为每次制作游戏的强制流程。

- 已确认原生 HTML、Godot 或 Phaser 且用户要求实现：可参考 [项目模板与工具](references/starter-projects.md)。仅在骨架适合当前创意时使用 `scripts/create_starter.py`，也可以从零实现或采用其他结构。不要把演示玩法当作用户已经选择的玩法。
- 需要具体生成提示词与外部交接：可参考 [多模态提示词库](references/multimodal-prompts.md)，按已确认美术方向修改示例。
- 整理开发计划与验收任务：可采用 [项目简报样例](assets/templates/brief.example.json) 和 `scripts/generate_game_docs.py`，也可直接撰写适合当前项目的文档。未实现的任务保留未勾选状态。
- 实际试玩时可参考或改写 [试玩记录表](assets/templates/playtest.md)，记录设备、观察、原话和证据，空表不能当作测试结果。

此版本包含参考资料、模板和脚本，迁移时携带完整技能目录或 ZIP。单独复制本文件不能获得其附带资源。若用户要求仅一个 MD，则将当前任务必要的参考内联，并明确不包含工程模板和可执行工具。

## 交付前复核

交付前核对：体验目标是否清楚，当前范围是否服务用户目标，流程是否覆盖该玩法实际需要的状态，技术选择是否有依据，资产是否有规格和提示词，工具能力边界是否诚实，新增内容是否有回归方案，验收标准是否可观察。
